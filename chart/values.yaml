# Defaults. Each service overrides only what differs, in apps/<name>-values.yaml.
replicas: 2
port: 8080
image: { repository: "", tag: "v1" }
service: { enabled: true }
probes: { http: true }
resources:
  requests: { cpu: 100m, memory: 128Mi }
  limits: { memory: 128Mi }
